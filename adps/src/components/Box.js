import React from "react";
import { useHttpClient } from '../httpClient/HttpClientContext';

//is what the client sees from all restaurants
//have a button to ordcer
function Box({ id, name, restaurant_id, quantity, tags, price }) {
/*
  return (
    //TO-DO: HTML Code how a Box should be represented
    );*/
}

export default Box;
